/** Fill color used while a combat telegraph is warning but not yet dangerous. */
export const TELEGRAPH_WINDUP_FILL_COLOR = 0xfbbf24

/** Stroke color used while a combat telegraph is warning but not yet dangerous. */
export const TELEGRAPH_WINDUP_STROKE_COLOR = 0xf59e0b

/** Fill color used while a combat telegraph is in its dangerous window. */
export const TELEGRAPH_DANGER_FILL_COLOR = 0xef4444

/** Stroke color used while a combat telegraph is in its dangerous window. */
export const TELEGRAPH_DANGER_STROKE_COLOR = 0xfef2f2

/** Ground telegraph fill alpha. */
export const TELEGRAPH_FILL_ALPHA = 0.28

/** Ground telegraph stroke alpha. */
export const TELEGRAPH_STROKE_ALPHA = 0.85

/** Ground telegraph stroke width in world pixels. */
export const TELEGRAPH_STROKE_WIDTH_PX = 2

/** Milliseconds before Lightning Bolt effect time to show danger styling. */
export const LIGHTNING_TELEGRAPH_DANGER_LEAD_MS = 120
