/** Camera configuration. Static canvas — no scroll or follow. */

/** Camera zoom level (1.0 = canvas fills the 1344×768 viewport). */
export const CAMERA_ZOOM = 1.0

/** Whether the camera follows the local player. False = static. */
export const CAMERA_FOLLOWS_PLAYER = false
