/** Starting gold for each player at match start. */
export const STARTING_GOLD = 15

/** Gold awarded per kill. */
export const KILL_REWARD = 5

/** Gold cost per shop item. */
export const SHOP_ITEM_COST = 10

/** Number of ability bar slots. */
export const ABILITY_BAR_SLOT_COUNT = 5

/** Number of quick item slots (Q, 6, 7, 8). */
export const QUICK_ITEM_SLOT_COUNT = 4

/** Maximum charge stack per quick-item type per slot. */
export const QUICK_ITEM_STACK_MAX = 9
